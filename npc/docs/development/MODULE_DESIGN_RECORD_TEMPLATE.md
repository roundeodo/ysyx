# <模块名称>：原理与电路结构

状态：<设计中 / 已实现>；对应源码：<链接>；更新日期：YYYY-MM-DD。

编写 RTL 前先填清以下内容；实现后同步校正。只保留适用内容，不强行填满空表或扩写未来计划。

## 1. 功能与边界

用一小段说明：模块解决什么问题，输入、输出是什么，哪些状态由它负责。
参数引用统一配置入口，仅列影响本模块结构的关键值。

## 2. 电路结构

按实际数据流说明组合运算、并行支路、反馈和寄存器边界，并给出相同的源码阅读顺序。
章节编号只表示阅读顺序，不能让人误以为每节都是一级流水线。

| 状态或流水级 | 保存什么、为什么需要 | 写入 / 保持 / 清除条件 |
| --- | --- | --- |
| <名称> | <作用> | <条件> |

## 3. 工作过程与边界条件

简述正常请求如何完成、延迟和吞吐是多少；补充反压、同时读写、复位、flush及错误处理。
存在状态机时，用“当前状态的输出、转移条件”表说明，RTL按输出、下一状态、时序更新排列。
纯组合模块不写状态表。

## 4. 关键取舍与验证

只写影响当前结构的选择：选了什么、理由、代价；必要时链接证据或一手资料。
列出关键正确性约束及对应测试入口。命令、原始数据、完整测量表放验证记录，这里只链接。
未解决且会影响实现的问题必须明确；不添加与当前模块无关的路线规划或重复检查表。
