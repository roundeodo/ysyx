# <模块名称>设计记录

状态：提议 | 已接受 | 已实现 | 已验证 | 已被替代

所有者：`<module_name>`

架构决策：`<Dxxx或pending>`

最后更新：YYYY-MM-DD

## 1. 问题和目标

- 需要解决的功能问题：
- 可测量的延迟、吞吐、容量、面积和验证目标：
- 本检查点明确不实现的能力：

## 2. 所有权边界

| 本模块拥有 | 其他模块拥有 |
| --- | --- |
| 本模块控制的状态、策略或协议 | 不得泄漏到本模块的状态、策略或协议 |

说明引入流水线、cache、超标量发射或乱序执行后，这条模块边界为何仍然成立。

## 3. 接口契约

| 接口 | 方向 | 事务开始/结束 | 稳定性规则 | 反压行为 |
| --- | --- | --- | --- | --- |
| `<name>` | 输入/输出 | `<valid && ready>` | `<payload稳定条件>` | `<由谁等待>` |

记录payload字段、位宽、顺序约束、最大未完成事务数、错误行为，以及是否允许同周期旁路。

## 4. 数据结构

对每个阵列、队列、表项或寄存器状态组分别记录：

- 保存什么信息，以及为什么需要这些信息。
- 表项数、相联度或位宽，以及索引的推导方法。
- 复位和失效行为。
- 读写端口，以及同周期读写冲突的语义。
- 未来扩大容量、端口或并发度时的迁移路径。

## 5. 工作过程

按周期描述完整事务。若使用状态机，描述顺序与RTL源码一致：先写输出行为，再写状态
转移条件，最后写寄存器更新。

## 6. 正确性不变量

- 列出与性能无关、任何时候都必须成立的性质。
- 写清复位、flush/redirect、异常、响应错误和反压下的行为。
- 标明每条性质由断言、定向测试、随机测试还是DiffTest检查。

## 7. 时序和快速路径

- 预期关键路径和寄存器边界：
- 候选fast bypass及其可阻塞的寄存器后备路径：
- 如何避免组合环、payload不稳定和高扇出：

## 8. 性能事件

| 事件 | 单周期脉冲或区间的精确定义 | 回答的性能问题 |
| --- | --- | --- |
| `<event>` | `<定义>` | `<问题>` |

## 9. 验证计划

- 模块级测试：
- 协议断言：
- 随机反压和随机延迟：
- 集成回归：
- 性能验收标准：

## 10. 开源设计研究

| 来源 | 研究的具体机制 | 采用/调整/拒绝 | 本地依据 |
| --- | --- | --- | --- |
| `<一手资料URL>` | `<具体机制>` | `<选择>` | `<理由>` |

## 11. 决策和备选方案

逐项记录非平凡选择、比较过的备选方案、当前证据，以及重新审视该决策所需的条件。

## 12. 仍需解决的问题

明确保留未解决问题，不能把尚未决定的策略直接固化到RTL中。

## 13. 实现检查表

- [ ] package类型和参数
- [ ] 子模块
- [ ] 父模块集成
- [ ] 断言和模块级测试
- [ ] 功能回归
- [ ] 性能实验和日志条目
- [ ] 架构计划更新
- [ ] 学习日志条目
