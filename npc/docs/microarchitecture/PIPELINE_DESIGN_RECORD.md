# RV32 执行流水线：原理与电路结构

当前工作树，2026-09-19。前端见[前端总览](FRONTEND_REWRITE_DESIGN_RECORD.md)，
访存见[D-cache 说明](DCACHE_DESIGN_RECORD.md)。本说明维护后端的实际寄存边界与控制职责。

## 指令怎样完成

普通指令经过组合译码、GPR 读取、操作数选择与前递，进入 ID/EX；EXU 组合执行后写入
结果寄存器，再经完成选择器、WB 寄存器和组合提交逻辑更新架构状态。
访存指令从 EXU 进入 LSU 的单项上下文，访存完成后直接参与完成选择，不经过普通结果寄存器。

| 模块 | 电路结构与源码顺序 |
| --- | --- |
| `riscv32_idu` | 指令字段/立即数 → opcode/funct 译码 → 异常归一化；纯组合 |
| `riscv32_regfile` | 31 项 GPR，两个组合读口、一个同步写口；x0 读零、写忽略 |
| `riscv32_hazard_ctrl` | 源/目的匹配 → 前递选择及 RAW 等待 → 串行化/结构等待 → 发出与 flush；纯组合 |
| `riscv32_operand_mux` | 默认 GPR/CSR 输入 → PC/立即数选择 → 按来源优先级前递 → 执行包；纯组合 |
| `riscv32_id_ex_reg` | 单项执行包及生产者类别；空闲或旧项交付时入口 ready |
| `riscv32_exu` | 并行整数、分支、CSR、地址运算 → 结果/异常选择 → 普通完成或 LSU 请求；纯组合 |
| `riscv32_ex_result_reg` | 一个结果寄存器及预测后继；输出端比较真实后继，产生纠错 |
| `riscv32_lsu` | 入口直通或上下文保持 → 对齐/请求生成和 load 格式化 → 输出 → 下一状态 → 状态与上下文更新 |
| `riscv32_completion_mux` | LSU 完成优先于普通结果，组合选择并返回 ready |
| `riscv32_wb_reg` | 一个可反压的完成寄存器；消费旧项的同沿可写新项 |
| `riscv32_commit` | 组合产生 commit 与 GPR 写口；异常抑制正常副作用，不再增加寄存级 |

旧独立 decode/register-read 寄存模块位于 `experiments/pipeline/`，不在当前编译清单中，
本版 core 不实例化它们。
源码段落表示阅读顺序，并行执行单元之间没有隐藏的串联流水级。

## 状态与前递

ID/EX 只保存一条指令，反压直接传回译码。有效位与宽数据分开更新；flush 清有效位，
无效数据不需要清零。结果级和 WB 在反压时保持，允许同沿消费与替换。

操作数在写入 ID/EX 前选择。当前 EX 生产者优先，其次结果寄存级、LSU 成功完成，最后 WB；
若最近生产者尚未完成，必须停顿，不能选更老的同名寄存器值。LSU 成功交付时直接向 ID/EX 前递；
故障和反压时继续阻塞相关指令。前递选择由冒险模块决定，数据 mux 由操作数准备模块拥有。

LSU 保存一次访存完成所需的 PC、指令、目的寄存器、地址、数据与异常信息。
三态为 `IDLE`、寄存请求派发、等待存储响应；异常/未对齐访问本地完成，不能发出存储请求。
空闲时普通请求直接派发；下游反压才进入请求保持状态。成功完成拍允许接收下一请求。

## 顺序、异常与中断

LSU 未完成时阻止年轻执行；成功交付 WB 的同拍，可让年轻普通结果写入空的结果寄存器，
但其最早下一拍才参与完成选择。顺序保证来自发出条件，不能仅靠完成 mux 的优先级。
EX 结果级持有有效异常时，即使 WB 可以接收，也必须保持年轻 ID/EX 指令并禁止其发射。
老异常继续进入 WB，由提交恢复统一清空年轻指令；这个组合阻塞条件不新增寄存器。
因此年轻 load/store 不会先进入无法取消的 LSU，非串行化异常也受同一规则保护。
SYSTEM 与 FENCE 类指令串行化，进入前等待老指令排空，提交前阻止年轻指令进入。

| 模块 | 状态与处理顺序 |
| --- | --- |
| `riscv32_csr_file` | M-mode 可变字段；读值重建与地址检查 → trap/mret/提交写入，优先级从左到右 |
| `riscv32_pmu` | 两个 64 位计数器，各拆高/低 32 位；读窗口 → 各计数器下一值与更新，软件写优先于自增 |
| `riscv32_interrupt_ctrl` | 保存架构恢复 PC；屏蔽新发出，等待后端与维护操作排空后受理定时中断 |
| `riscv32_trap_ctrl` | 同步异常、mret、定时中断按顺序选择，组合生成 CSR 更新与架构重定向 |
| `riscv32_redirect_mux` | 组合优先选择；提交恢复优先于 FENCE.I 和执行纠错，不增加恢复周期 |

CSR 只保存实际可变字段；计数器与仿真性能统计分离。中断不伪造退休指令。
详细中断边界见[定时中断说明](TIMER_INTERRUPT_DESIGN_RECORD.md)。

## FENCE.I 与 core 装配

`riscv32_fence_i_ctrl` 拥有四态维护过程：空闲 → 排空前端 → D-cache clean →
I-cache invalidate。各请求保持至完成。一个“已展示但被反压”的取指记录允许旧请求完成，
后续新请求暂停；清理顺序保证脏指令数据先写回，再让取指重新填充。

该模块把 core 中已有状态与组合条件集中起来，不新增状态。操作数准备模块也不增加寄存器。
core 按前端、译码/前递、执行/访存、完成/提交、恢复/维护、仿真观察组织连接。
D-cache clean 错误目前由断言报告，尚无软件可见的维护异常，不应表述为已实现。

## 验证入口

译码、EXU、LSU、流水控制、CSR/PMU、访存与中断分别有定向回归，系统行为使用 RV32 DiffTest。
本轮优化的配置、源码核对与结果见[周期优化验证](../verification/RV32_CYCLE_OPT_2026-09-19.md)。
[历史设计与测量](archive/PIPELINE_DESIGN_RECORD_BEFORE_2026-09-16.md)仅供追溯。
